{
  "name": "codex-sillytavern-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local OpenAI-compatible bridge from SillyTavern to Codex app-server.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "codex-sillytavern-bridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
